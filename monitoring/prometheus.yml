scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s # 5초마다 데이터 수집
    static_configs:
      # EC2 호스트에서 돌아가는 스프링 부트에 접속하기 위해 host.docker.internal 사용
      - targets: ['host.docker.internal:8080']